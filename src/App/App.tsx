import { Header } from '@components/Header'
import { Router } from '@components/Router'
import React from 'react'

export const App = () => {
	return (
		<div>
			App
			<Header />
			<Router />
		</div>
	)
}
